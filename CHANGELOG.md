## 2.0

- 发布 MusicConverter 2.0 正式版。
- 完善 GitHub Actions 自动构建与正式版发布流程。
- 保留现有音乐解密、格式转换、批量处理、音频编辑和更新功能。

## 1.10.0

- 参考 ACE 工具箱的 MIUI / Material 卡片层级重新优化整体 UI。
- 首页保留中央主操作区，同时升级为 Hero 音乐卡片与两列功能中心。
- 批量页将扫描、任务状态、工具拆分为独立分组卡片。
- 工具项改为类似设置页的集中分组结构，减少连续大按钮堆叠。
- 介绍页改为横向 App Hero、分组能力列表、软件更新和信息区域。
- 日间主题统一采用蓝色强调色，夜间主题保持紫色。
- 底部导航选中态改为 Primary Container 风格。
- 日间模式状态栏 / 导航栏图标切换为深色，提升可读性。
- 所有现有业务逻辑保持不变。

## 1.9.4

- 用户授予“所有文件访问权限”后，批量页额外显示“全盘扫描”按钮。
- 未授权时不显示全盘扫描，原有 SAF 选择目录扫描保持不变。
- 全盘扫描直接遍历已挂载共享存储，可发现未被 MediaStore 索引的加密音乐文件。
- 全盘扫描遵守“扫描过滤”设置，并跳过 `Music/MusicConverter` 输出目录。
- 为全盘扫描结果补充 `file://` 输入、删除和源文件置换支持。
- 用户点击“全盘扫描”后仍会二次确认，不会授权后自动扫描。

## 1.9.3

- 修复腾讯文档备用更新源无法识别“正文直接放 JSON”的问题。
- 腾讯文档备用检查优先请求 `dop-api/opendoc` 公开数据，再回退分享页。
- 支持从 `clientVars`、`collab_client_vars`、`initialAttributedText` 等嵌套结构递归提取正文。
- 支持富文本把 JSON 拆成多个字符串片段后重新拼接。
- 增加 HTML/JS 转义兼容及按字段兜底提取。
- 保留 `MUSICCONVERTER_UPDATE_BEGIN/END` 标记块解析兼容。
- GitHub 成功时仍严格不访问腾讯文档。

## 1.9.2

- 修复批量页“所有文件访问权限”按钮未真正加入布局的问题。
- “所有文件访问权限”固定显示在批量页工具区顶部。
- 日间模式下 AlertDialog 改用系统浅色 Material Dialog 主题。
- 扫描过滤和批量转换弹窗不再出现深灰底 + 低对比文字。
- 日间模式弹窗操作按钮改为蓝色强调。
- 批量转换弹窗加入 ScrollView，小屏设备可完整滚动操作。
- 夜间模式继续使用深色 Dialog。

## 1.9.1

- 日间主题下首页 `LOCAL · OFFLINE` 改为蓝色文字、浅蓝底和蓝色边框。
- 日间主题下介绍页顶部卡片改为浅蓝色背景与蓝色边框。
- 介绍页版本号改为蓝色强调，说明文字调整为更适合浅色背景的蓝灰色。
- 夜间主题继续保持原有紫色风格。

## 1.9.0

- 新增日间 / 夜间主题切换能力。
- “关于与设置”页面新增主题模式按钮。
- 主题偏好使用 SharedPreferences 持久化。
- 切换后通过 `recreate()` 立即刷新当前界面。
- 首页、批量页、关于页、音频编辑页和波形图跟随主题切换。
- 夜间主题保持现有深色视觉；日间主题使用浅灰背景、白色卡片与紫色强调色。

## 1.8.9

- 首页“音乐工作台”标题从 29sp 放大到 35sp。
- 当前音乐卡片与“选择音乐文件”按钮组成中央主操作区。
- 根据设备屏幕高度动态计算顶部留白，使主操作区更接近屏幕中部。
- 当前音乐卡片、音乐图标和主按钮同步放大。
- 其他功能逻辑保持不变。

## 1.8.8

- 新增用户可选的“所有文件访问权限”入口。
- Android 11+ 使用 `MANAGE_EXTERNAL_STORAGE`，由用户主动进入系统设置授权。
- 权限按钮会显示“已授权 / 未授权”，从系统设置返回后自动刷新。
- Android 10 及以下无需该特殊权限。
- 不开启该权限时，原有 SAF 目录选择、扫描、转换和音频编辑均可继续使用。
- 不会在首次启动时强制申请该权限。

## 1.8.7

- 音频编辑界面新增波形图预览。
- 波形图支持显示当前剪辑选区和试听播放位置。
- 拖动起点/终点滑块时，波形中的选区会同步更新。
- 试听时新增播放进度竖线，暂停时保留当前位置。
- 优先使用 `MediaCodec` 解码生成波形，失败时自动回退到文件采样模式。
- 保留原有剪辑、试听、导出、源文件删除/置换功能。

## 1.8.6

- 优化整体窗口 UI，提升层级、留白和一致性。
- 卡片改为更克制的深色中性风格，减少紫色堆叠。
- 底部导航改为更轻、更悬浮的视觉。
- 首页改为“音乐工作台”，增加本地离线处理标识。
- 批量页改为“批量处理”，扫描过滤入口更清晰。
- 忽略格式弹窗文案和整体风格统一。
- 不修改解密、转换、批量、更新等功能逻辑。

## 1.8.5.2.1

- 修复 `MainActivity.kt` 中 `ScrollView.LayoutParams` 无法解析导致的 Kotlin 编译失败。
- 改为 `FrameLayout.LayoutParams`。
- 忽略格式复选框功能保持不变。

## 1.8.5.2

- 修复“忽略格式”弹窗只有说明文字、没有可选格式列表的问题。
- 改为自定义 `ScrollView + CheckBox`，兼容 MIUI / HyperOS。
- 每个格式均可独立勾选或取消。
- “全部不忽略”仅清空当前勾选，点击“保存”后才写入设置。

## 1.8.5.1

- 修复 `MainActivity.kt` 中 18 处 `BuildConfig` 无法解析导致的 Kotlin 编译失败。
- 当前版本信息改为通过 Android `PackageManager` 获取。
- 不再要求 app 模块生成 `BuildConfig`。
- GitHub 主更新源、腾讯文档备用源、强制更新及忽略更新逻辑均保持不变。

## 1.8.5

- 版本检查调整为 GitHub 严格优先。
- GitHub 可正常访问并解析时，不再请求腾讯文档。
- GitHub 网络失败、HTTP 失败或清单解析失败时，自动调用腾讯文档备用源。
- 其他更新策略保持不变。

## 1.8.4

- 新增腾讯文档备用版本检查地址。
- GitHub 检查失败时自动回退到腾讯文档。
- 腾讯文档兼容正文 JSON、`MUSICCONVERTER_UPDATE_BEGIN/END` 标记块及页面内嵌 JSON。
- 保留夸克 / 蓝奏云下载源、强制更新门禁和忽略更新策略。

## 1.8.3

- 新增强制更新启动门禁。
- 强制更新弹窗不可取消，只提供下载更新或退出应用。
- 强制状态本地持久化，防止断网重启绕过。
- 从外部下载页返回且仍是旧版本时自动再次拦截。
- 普通可选更新的“忽略 2 个版本”逻辑保持不变。

## 1.8.2

- 新增“忽略更新”选项。
- 忽略后只跨 1 个版本不再自动提醒，跨 2 个版本后恢复提醒。
- 手动检查更新始终显示最新可用版本。
- 强制更新不可忽略。
- 更新下载源改为二级选择，保留夸克与蓝奏云。

## 1.8.1

- 新增蓝奏云备用更新链。
- 选择蓝奏云下载时自动复制提取密码 `6666`。
- 更新弹窗同时支持夸克与蓝奏云两个下载入口。
- GitHub 继续作为远程版本清单来源。

# CHANGELOG

## 1.8.0

- GitHub 远程版本清单检查。
- 夸克网盘 APK 更新入口。
- 每日一次自动检查与手动检查。
- 第三页新增“软件更新”区域。
